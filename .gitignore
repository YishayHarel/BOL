__pycache__/
*.pyc
.DS_Store
.venv/
venv/
.env

# Local runtime folders
input/
output/

# Real batches / client data — never commit (public repo)
samples/*.pdf
*.pdf

# Real candidate list (has actual vendor names); commit only the example
candidates.json

# Store dump + generated index (client data — never commit)
store_index.json
*.xlsx
data/

# Claude Code local settings
.claude/settings.local.json

# Local-only demo web UI (kept out of the repo — functionality only)
bol_splitter/web.py
docker-compose.override.yml
